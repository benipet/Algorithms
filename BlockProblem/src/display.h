#include "node.h"
#include "node.c"

typedef struct _construction constr;

/*
	Prototypes
*/

//Display Positions
void display_positions(pos* table);
//Display BlockNode
void display_blockNode(blck* instance);
//Display PositionNode
void display_positionNode(pos* instance);
